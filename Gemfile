source 'https://rubygems.org'

# This points to our patched version of `ruby-elm-compiler`.
# We can revert to using the gemspec again once our patch gets merged.
gem 'elm-compiler', git: 'git@github.com:RBM-Technologies/ruby-elm-compiler'
